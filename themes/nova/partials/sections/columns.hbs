<div class="section-columns"{{#if (or bgColor textColor)}} style="{{#if bgColor}}--section-bg:{{bgColor}};{{/if}}{{#if textColor}}--section-text:{{textColor}};{{/if}}"{{/if}}>
  <div class="section-columns__grid">
    <div class="section-columns__col">{{sanitized_html leftContent}}</div>
    <div class="section-columns__col">{{sanitized_html rightContent}}</div>
  </div>
</div>
